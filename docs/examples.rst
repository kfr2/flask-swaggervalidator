These examples assume your project has a swagger.yaml file in its root.
For example, the file may look like:

.. literalinclude:: examples/swagger.yaml

The decorators can be used in a manner like:

.. literalinclude:: examples/api.py
